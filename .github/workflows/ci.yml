name: CI

on:
  push:
    branches: [main, develop, feature/**]
  pull_request:
    branches: [main, develop]

jobs:
  ci:
    name: CI
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm install

      - name: Setup Supabase CLI
        uses: supabase/setup-cli@v1
        with:
          version: latest

      - name: Start Supabase
        run: npx nx run supabase:start

      - name: Apply migrations
        run: npx nx run supabase:run-command --command="supabase db reset"
        env:
          CI: true

      - name: Wait for Supabase to be ready
        run: |
          echo "Waiting for Supabase API to be ready..."
          sleep 10

          # Check if Supabase REST API is responding
          max_attempts=20
          attempt=0
          until curl -f http://127.0.0.1:54321/rest/v1/ 2>/dev/null || [ $attempt -eq $max_attempts ]; do
            echo "Attempt $((attempt+1))/$max_attempts - Waiting for Supabase API..."
            sleep 3
            attempt=$((attempt+1))
          done

          if [ $attempt -eq $max_attempts ]; then
            echo "Supabase API check failed"
            echo "Checking docker containers..."
            docker ps -a
            exit 1
          fi

          echo "Supabase is ready!"

      - name: Verify migrations applied
        run: |
          echo "Verifying database migrations were applied..."

          # Check if games table exists via REST API
          response=$(curl -s -o /dev/null -w "%{http_code}" \
            -H "apikey: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZS1kZW1vIiwicm9sZSI6ImFub24iLCJleHAiOjE5ODM4MTI5OTZ9.CRXP1A7WOeoJeXxjNni43kdQwgnWNReilDMblYTn_I0" \
            "http://127.0.0.1:54321/rest/v1/games")

          if [ "$response" = "200" ]; then
            echo "✅ Migrations verified: games table exists"
          else
            echo "❌ Migration check failed: games table not found (HTTP $response)"
            echo "Checking database tables..."
            docker exec supabase_db_local psql -U postgres -d postgres -c "\dt public.*" || true
            exit 1
          fi

      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium

      - name: Run E2E tests (Chromium only)
        run: npx nx e2e web-e2e -- --project=chromium --workers=4
        env:
          CI: true

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: |
            dist/.playwright/apps/web-e2e/playwright-report/
            apps/web-e2e/playwright-report/
          retention-days: 30
          if-no-files-found: ignore

      - name: Upload test screenshots
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-screenshots
          path: |
            dist/.playwright/apps/web-e2e/test-output/
            apps/web-e2e/test-results/
          retention-days: 7
          if-no-files-found: ignore

      - name: Stop Supabase
        if: always()
        run: npx nx run supabase:stop || true
